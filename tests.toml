test_format = 1.0

[default]

# dex support
preinstall = """
sudo yunohost tools update apps
sudo yunohost domain add dex.domain.tld
sudo yunohost app install dex -f -a "domain=dex.domain.tld&path=/&oidc_name=&oidc_secret=&oidc_callback"
"""

# GTS don't support it
exclude = [ "change_url" ]

# -------------------------------
# Default args to use for install
# -------------------------------

args.dex = "dex"
args.admin = "xana"
args.email = "ci-test@example.com"
args.password = "vYh8io7r*@P&zyo!6ZhBqQQ%8M5D4M"
# false by default
args.accounts_registration_open = 1
# true by default
args.accounts_reason_required = 0

# -------------------------------
# Commits to test upgrade from
# -------------------------------

test_upgrade_from.388d9cf9.name = "Upgrade from 0.19.1~ynh1"

# -------------------------------
# additional tests suite
# -------------------------------

[nowasm_test]
only = ["install.root"]
preinstall = """
touch /var/www/nowasm_test
sudo yunohost tools update apps
sudo yunohost domain add dex.domain.tld
sudo yunohost app install dex -f -a "domain=dex.domain.tld&path=/&oidc_name=&oidc_secret=&oidc_callback"
"""

args.dex = "dex"
args.admin = "xana"
args.email = "ci-test@example.com"
args.password = "vYh8io7r*@P&zyo!6ZhBqQQ%8M5D4M"
args.accounts_registration_open = 1
args.accounts_reason_required = 0
